# 2026-03-03

- Investigated Telegram message duplication report.
- Checked gateway logs: OpenClaw only issued one `telegram sendMessage ok` for the initial /start reply (message id 2) and only one OpenClaw gateway process was running.
- Noted a Telegram provider restart shortly before the first observed duplication: `health-monitor: restarting (reason: stale-socket)`.
- Enabled debug logging for deeper Telegram diagnostics via config patch:
  - `logging.level=debug`, `logging.consoleLevel=info`, `logging.redactSensitive=tools`.
- After reproduction attempt, debug logs showed a single inbound Telegram update (`update_id: 565125761`, text: "Happened now") and no duplicate inbound update_id or duplicate outbound sendMessage entries in server logs; suspected Telegram client/UI duplication (or two distinct events that look identical).
- Minor ops note: `openclaw logs --since` is not a supported flag in this version; use `--limit` / file tailing.
