# 2026-03-08

## Migración LLM Mailbot: FreeRide -> Router interno multi-provider

- Se retiró la dependencia operativa de FreeRide para el Mailbot.
- Se implementó `src/llm_router.py` con:
  - Failover por proveedor y por key.
  - Cooldown por key (rate-limit/auth) y por proveedor.
  - Circuit breaker por proveedor.
  - Fallback de modelos.
  - Estado persistente en `.runtime/llm_router_state.json`.
- `src/llm_service.py` ahora enruta draft/spam vía router (`task=draft|spam`).
- `src/main.py` valida que haya al menos un proveedor LLM configurado.
- `.env` y `.env.example` actualizados con configuración `LLM_PROVIDER_ORDER` y parámetros de resiliencia.
- README y scripts `run_track1.sh` / `run_track2.sh` actualizados a router interno.
- Se agregaron tests unitarios: `tests/unit/test_llm_router_policy.py`.
- Verificación local: tests del router pasan (4/4).
- Intento de desinstalación de `freeride`/`freeride-watcher`: no estaban instalados en este host.

Pendiente recomendado:
- Correr test de integración/canary con tráfico real para medir fallback depth y latencias en producción.
